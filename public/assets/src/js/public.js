"use strict";

jQuery(function ($) {
	const globalParams = {
		/**
		 * @const global_params
		 * @property ajax_url
		 * @property home_url
		 * */
		ajaxUrl: global_params.ajax_url,
		homeUrl: global_params.home_url
	};
});
